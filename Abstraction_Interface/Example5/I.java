package com.Abstraction_Interface.Example5;

// Note: one interface always extends another interface
// Note: class is always implements the interface
// Note : we can not  create object of interface , but we can interface as reference variable
public interface I {

	int x = 10;
	/*int z; Note= in interface we always need to initialized the variable always ,  otherwise it will give get compile time error saying 
	              (Error:The blank final field z may not have been initialized)*/
	
}
